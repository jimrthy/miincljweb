(ns com.jimrthy.cluster-web.admin.db.counter-test
  "Placeholder in case I ever decide that a counter's a good idea"
  (:require [clojure.test :refer [are deftest is testing use-fixtures]]))

(deftest check-counter
  []
  (is true "TODO: Write this."))
